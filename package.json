{
  "name": "altleads-crm",
  "version": "1.0.0",
  "description": "AltLeads CRM — single Hostinger Node app: serves the React web app AND the email API.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "build": "node new-code/notify-service/gen-build-info.cjs && npm install && cd new-code/web && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "nodemailer": "^6.9.0",
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.4.0",
    "@supabase/supabase-js": "^2.49.4"
  }
}
