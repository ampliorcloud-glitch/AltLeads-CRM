{
  "name": "altleads-crm",
  "version": "1.0.0",
  "description": "AltLeads CRM — single Hostinger Node app: serves the React web app AND the email API.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "build": "npm install && cd new-code/web && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "nodemailer": "^6.9.0",
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0"
  }
}
